# Chemistry Dashboard Corrections — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Apply Muhannad's 14 actionable corrections to the chemistry dashboard (jam #15 deferred), centred on collapsing the duplicate name-grouping systems into one category-aware source of truth.

**Architecture:** `categories.name_group()` becomes the single, category-aware sample-name grouper; the cleaner writes `sample_name_group`; the dashboard reads that column instead of its JS duplicate. Category/classification fixes live in `categories.py` + `clean_chemistry.py`; label/YoY/rendering fixes live in `build_dashboard.py`.

**Tech Stack:** Python 3 (pandas, pyarrow), self-contained HTML+JS dashboard (Plotly) generated by `build_dashboard.py`. No test framework installed — tests are plain `assert` scripts run with the venv Python.

## Global Constraints

- Venv Python: `/home/bioinfo/Documents/Data-Analysis-Muhannad/microbiology/.venv/bin/python` (shared with microbio). Referenced below as `$PY`.
- Scope is strictly `chemistry/` plus its byte-identical mirror `clean/chemistry/`. Do not touch `microbiology/`.
- Water stays **2 canonical classes** (potable / non-potable). Name-group display names never change a row's canonical category.
- 5-sector taxonomy incl. **Central** — never drop it.
- Aflatoxin: only *Aflatoxin Total* has a regulatory limit.
- After any parquet-affecting change: rebuild parquets, then `cp chemistry/cleaned/*.parquet clean/chemistry/`, then rebuild the dashboard.
- Run all commands from `/home/bioinfo/Documents/Data-Analysis-Muhannad/chemistry`.
- Set `$PY` first: `PY=/home/bioinfo/Documents/Data-Analysis-Muhannad/microbiology/.venv/bin/python`

---

### Task 1: Category-aware `name_group()`

Rewrite `name_group()` to be category-scoped and comprehensive (fixes #1, #2, #3, #4, #6, #7, #11), and update its one caller.

**Files:**
- Modify: `chemistry/scripts/categories.py` (replace `name_group`, lines 337-349; add module-level regex tables near it)
- Modify: `chemistry/scripts/clean_chemistry.py:407` (pass category + sample_id)
- Test: `chemistry/scripts/tests/test_categories.py` (new)

**Interfaces:**
- Produces: `name_group(sample_name, category=None, sample_id=None) -> str | None` — returns a display/group label, or `None` to fall back to the original name. `category` is the canonical category string (e.g. `categories.C_FRVEG`).
- Consumes: existing module constants `C_FRVEG, C_FISH, C_MEAT, W_TAP, W_FILTER` and helpers `_norm`, `_prefix`.

- [ ] **Step 1: Write the failing tests**

Create `chemistry/scripts/tests/test_categories.py`:

```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import categories as C

def check(got, want, msg):
    assert got == want, f"{msg}: got {got!r}, want {want!r}"

# #11 — fruit family must NOT fire on a dairy product
check(C.name_group("حليب جمل فراولة 2 عينة خاصة", C.C_DAIRY, "milk-0002-r01"),
      "حليب جمل فراولة", "dairy strawberry must not collapse to fruit")
# fruit family DOES fire inside fruit & veg
check(C.name_group("فراولة كوري", C.C_FRVEG, "str-0001-r01"), "فراولة",
      "strawberry in fruit&veg -> فراولة")
# #3 — meat token-order-insensitive
a = C.name_group("لحم عجل كتف بلدي", C.C_MEAT, "meat-1")
b = C.name_group("كتف بلدي عجل لحم", C.C_MEAT, "meat-2")
check(a, b, "meat word-order variants must merge")
# different cut stays separate
assert C.name_group("لحم فخذ عجل بلدي", C.C_MEAT, "m3") != a, "different cut must not merge"
# #4 — every filter-water variant collapses
for n in ["مياة فلتر للعجانة", "موية فلتر", "مياه فلتر للطبخ م ك", "ماء فلتر"]:
    check(C.name_group(n, C.W_POTABLE, "ubot-1-r01"), C.W_FILTER, f"filter collapse: {n}")
# #7 — fish families
check(C.name_group("سمك دنيس ني م.ك", C.C_FISH, "fish-1"), "دنيس", "fish family دنيس")
check(C.name_group("روبيان جامبو بحر", C.C_FISH, "fish-2"), "ربيان", "shrimp family ربيان")
# #1/#2 — nameless water by prefix
check(C.name_group("", C.W_POTABLE, "ubot-0007-r01"), C.W_TAP, "nameless ubot -> tap")
check(C.name_group("", C.W_POTABLE, "bot-0007-r01"), "مياه معبأة", "nameless bot -> bottled")
# #6 — generic suffix/number stripping
check(C.name_group("حليب سادة 3", C.C_DAIRY, "milk-9"), "حليب سادة", "strip trailing number")

print("ALL PASS")
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `$PY scripts/tests/test_categories.py`
Expected: `AssertionError` (current `name_group` takes only `sample_name` and returns `"فراولة"` for the dairy case) — or a `TypeError` on the 3-arg call.

- [ ] **Step 3: Implement the category-aware `name_group`**

In `chemistry/scripts/categories.py`, replace the current `name_group` (lines 337-349) with the following. `re` is already imported at the top of the module.

```python
# --------------------------------------------------------- name-group tables
# Re-test / unit markers the lab appends: م.ك / م ك / م-ك / م - ك.
_MARKER_RE  = re.compile(r"م[\s.\-]*ك\b")
_DIGIT_RE   = re.compile(r"\d+")
_PRIVATE_RE = re.compile(r"عين[ةه]\s*خاص[ةه]")

_FRUIT_FAMILIES = [
    (re.compile(r"ليمون"), "ليمون"), (re.compile(r"برتقال"), "برتقال"),
    (re.compile(r"يوسف"), "يوسفي"), (re.compile(r"فراول[ةه]"), "فراولة"),
    (re.compile(r"تفاح"), "تفاح"), (re.compile(r"عنب"), "عنب"),
    (re.compile(r"بصل"), "بصل"), (re.compile(r"طماط[مه]|بندورة"), "طماطم"),
    (re.compile(r"خس"), "خس"), (re.compile(r"شط[ةه]"), "شطة"),
    (re.compile(r"فلفل"), "فلفل"),
]
_FISH_FAMILIES = [
    (re.compile(r"روبيان|ربيان"), "ربيان"), (re.compile(r"سلمون"), "سلمون"),
    (re.compile(r"دنيس"), "دنيس"), (re.compile(r"هامور"), "هامور"),
    (re.compile(r"كنعد"), "كنعد"), (re.compile(r"شعور"), "شعور"),
    (re.compile(r"بلطي"), "بلطي"), (re.compile(r"بوري"), "بوري"),
    (re.compile(r"ساردين"), "ساردين"), (re.compile(r"ناجل"), "ناجل"),
    (re.compile(r"كابوريا|سلطعون"), "كابوريا"), (re.compile(r"فيلي[ةه]"), "سمك فيليه"),
    (re.compile(r"محار"), "محار"),
]


def _clean_suffixes(s: str) -> str:
    """Strip re-test markers, standalone numbers, «عينة خاصة»; squeeze spaces."""
    s = _MARKER_RE.sub(" ", s)
    s = _DIGIT_RE.sub(" ", s)
    s = _PRIVATE_RE.sub(" ", s)
    return " ".join(s.split())


def name_group(sample_name, category=None, sample_id=None) -> str | None:
    """Category-aware display-name grouping. Returns a group label, or None to
    keep the caller's original name. Never changes canonical category — this is
    a display/rollup label only (Muhannad 2026-07-16)."""
    s = _norm(sample_name)

    # #1/#2 — nameless water rows take their name from the id prefix.
    if not s:
        pfx = _prefix(sample_id)
        if pfx == "ubot":
            return W_TAP            # «مياه الحنفية»
        if pfx == "bot":
            return "مياه معبأة"
        return None

    # #4 — every filter-water spelling/purpose variant collapses to one label.
    if "فلتر" in s:
        return W_FILTER            # «مياه فلتر»

    # #11 — fruit families fire ONLY inside fruit & veg, so flavoured milk
    # («حليب جمل فراولة») is never mislabelled as the fruit.
    if category == C_FRVEG:
        for rx, lab in _FRUIT_FAMILIES:
            if rx.search(s):
                return lab

    # #7 — fish sub-variants.
    if category == C_FISH:
        for rx, lab in _FISH_FAMILIES:
            if rx.search(s):
                return lab
        return _clean_suffixes(s) or None

    # #3 — meats: token-order-insensitive key merges reorderings + markers.
    if category == C_MEAT:
        toks = _clean_suffixes(s).split()
        return " ".join(sorted(toks)) or None

    # #6 — everything else: strip trailing numbers / «عينة خاصة» / markers.
    return _clean_suffixes(s) or None
```

- [ ] **Step 4: Update the caller**

In `chemistry/scripts/clean_chemistry.py`, change line 407 from:

```python
            grp = categories.name_group(rec.get("sample_name"))
```

to:

```python
            grp = categories.name_group(rec.get("sample_name"), canon,
                                        rec.get("sample_id"))
```

(`canon` is already defined two lines above at `clean_chemistry.py:405`.)

- [ ] **Step 5: Run tests to verify they pass**

Run: `$PY scripts/tests/test_categories.py`
Expected: `ALL PASS`

- [ ] **Step 6: Commit**

```bash
git add chemistry/scripts/categories.py chemistry/scripts/clean_chemistry.py chemistry/scripts/tests/test_categories.py
git commit -m "Chem: category-aware name_group (fixes flavoured-milk-as-fruit, meat/fish/filter merges, nameless water)"
```

---

### Task 2: كشنة → spices (#14)

**Files:**
- Modify: `chemistry/scripts/categories.py:57` (the كشنة entry in `NAME_OVERRIDE`)
- Test: `chemistry/scripts/tests/test_categories.py` (append)

**Interfaces:**
- Consumes: `classify(section, raw_category, sample_name, sample_id)` (unchanged signature).

- [ ] **Step 1: Add the failing test**

Append to `chemistry/scripts/tests/test_categories.py` (before the final `print`):

```python
# #14 — كشنة is a spice, not Ready-to-Eat
cat, _ = C.classify("food_chemistry", None, "كشنة بهارات", "spic-0001-r01")
check(cat, C.C_SPICE, "كشنة -> spices")
```

- [ ] **Step 2: Run to verify it fails**

Run: `$PY scripts/tests/test_categories.py`
Expected: `AssertionError: كشنة -> spices: got 'الأطعمة الجاهزة للأكل', want 'البهارات والصوصات'`

- [ ] **Step 3: Change the override**

In `chemistry/scripts/categories.py`, change the `NAME_OVERRIDE` entry (line 57) from:

```python
    ("كشنة", C_RTE),
```

to:

```python
    ("كشنة", C_SPICE),   # Muhannad 2026-07-16: كشنة is a spice, not RTE
```

- [ ] **Step 4: Run to verify it passes**

Run: `$PY scripts/tests/test_categories.py`
Expected: `ALL PASS`

- [ ] **Step 5: Commit**

```bash
git add chemistry/scripts/categories.py chemistry/scripts/tests/test_categories.py
git commit -m "Chem: reclassify كشنة from Ready-to-Eat to spices (#14)"
```

---

### Task 3: Drop «أخرى» / Others rows in the cleaner (#13)

**Files:**
- Modify: `chemistry/scripts/clean_chemistry.py` (insert after the null-id junk drop, after line 498, before `out_path =` on line 500)

**Interfaces:**
- Consumes: `records` list of dicts each carrying `sample_category_canonical`; `categories.C_OTHER`; `audit["flags"]`.

- [ ] **Step 1: Insert the drop filter**

In `chemistry/scripts/clean_chemistry.py`, immediately before line 500 (`out_path = CLEAN_DIR / ...`), insert:

```python
    # Drop «أخرى» (Others) — Muhannad 2026-07-16: genuinely-unclassified
    # leftovers are excluded from pipeline output entirely.
    before = len(records)
    records = [r for r in records
               if r.get("sample_category_canonical") != categories.C_OTHER]
    if before - len(records):
        audit["flags"]["others_dropped"] = before - len(records)
```

- [ ] **Step 2: Verify by running one section and checking the flag**

Run:
```bash
$PY scripts/clean_chemistry.py --section food_chemistry
$PY -c "import pandas as pd,glob; df=pd.concat([pd.read_parquet(f) for f in glob.glob('cleaned/chem_food_chemistry_*.parquet')]); import sys; sys.path.insert(0,'scripts'); import categories as C; n=(df['sample_category_canonical']==C.C_OTHER).sum(); print('OTHER rows remaining:', n); assert n==0, 'Others not dropped'; print('OK')"
```
Expected: `OTHER rows remaining: 0` then `OK`. (Console also prints an `others_dropped=` count for sections that had any.)

- [ ] **Step 3: Commit**

```bash
git add chemistry/scripts/clean_chemistry.py
git commit -m "Chem: drop «أخرى»/Others rows from pipeline output (#13)"
```

---

### Task 4: Rebuild all parquets + mirror, verify data-level fixes

**Files:**
- Regenerate: `chemistry/cleaned/*.parquet`, `clean/chemistry/*.parquet`
- Test: `chemistry/scripts/tests/verify_data.py` (new)

**Interfaces:**
- Consumes: the 12 rebuilt parquets in `cleaned/`.

- [ ] **Step 1: Rebuild every section + copy the mirror**

Run:
```bash
$PY scripts/clean_chemistry.py --section all
cp cleaned/*.parquet ../clean/chemistry/
```
Expected: 12 `wrote cleaned/chem_*.parquet` lines, no traceback.

- [ ] **Step 2: Write the data verification script**

Create `chemistry/scripts/tests/verify_data.py`:

```python
import sys, os, glob
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import pandas as pd, categories as C
A = pd.concat([pd.read_parquet(f) for f in glob.glob("cleaned/*.parquet")],
              ignore_index=True)

# #13 — no Others anywhere
assert (A["sample_category_canonical"] == C.C_OTHER).sum() == 0, "Others still present"

# #11 — no dairy row grouped under a fruit-family label
FRUIT = {"ليمون","برتقال","يوسفي","فراولة","تفاح","عنب","بصل","طماطم","خس","شطة","فلفل"}
dairy = A[A["sample_category_canonical"] == C.C_DAIRY]
bad = dairy[dairy["sample_name_group"].isin(FRUIT)]
assert bad.empty, f"dairy rows mislabelled as fruit: {bad['sample_name'].tolist()[:5]}"

# #4 — all filter water shares one group label
filt = A[A["sample_name"].astype(str).str.contains("فلتر", na=False)]
assert set(filt["sample_name_group"].unique()) == {C.W_FILTER}, \
    f"filter water not merged: {filt['sample_name_group'].unique()}"

# #1/#2 — nameless water rows got a name from their prefix
nameless = A[A["sample_name"].astype(str).str.strip().isin(["", "nan", "<NA>", "None"])]
ub = nameless[nameless["sample_id"].astype(str).str.startswith("ubot")]
if len(ub):
    assert set(ub["sample_name_group"]) <= {C.W_TAP}, "nameless ubot not named"
print("DATA VERIFY OK  (rows:", len(A), ")")
```

- [ ] **Step 3: Run the verification**

Run: `$PY scripts/tests/verify_data.py`
Expected: `DATA VERIFY OK  (rows: ...)`

- [ ] **Step 4: Commit the regenerated data**

```bash
git add chemistry/cleaned/*.parquet clean/chemistry/*.parquet chemistry/scripts/tests/verify_data.py
git commit -m "Chem: rebuild parquets with new grouping + Others drop; add data verify"
```

---

### Task 5: Dashboard reads `sample_name_group`; delete JS duplicate (#3/#6/#7/#11)

**Files:**
- Modify: `chemistry/scripts/build_dashboard.py` — payload cols (~line 240) + row tuple (~line 194); `renderTopSubtypes` (line 1341); delete `SAMPLE_NAME_FAMILY`/`canonName` (lines 1311-1331)

**Interfaces:**
- Produces: payload column `sample_name_group` (appended last so existing `COLS` indices are unchanged).
- Consumes: `filteredRows()`, `COLS`.

- [ ] **Step 1: Add `sample_name_group` to the row tuple**

In `build_dashboard.py`, append the new element at the **end** of the row `.append([...])` tuple so existing `COLS` indices stay stable. The tuple ends with the `_to_gso(...)` call closing `),` on the line before `])` (~line 228). Change:

```python
                ),
            ])
```

to:

```python
                ),
                _val(getattr(r, "sample_name_group", None))
                  if "sample_name_group" in df.columns else _val(getattr(r, "sample_name", None)),
            ])
```

- [ ] **Step 2: Add the column name to the `cols` list**

In the `cols` list (lines 240-243), append `"sample_name_group"` as the last entry:

```python
        "cols": ["year","date","year_month","sample_id","sample_name","sample_category",
                 "facility","municipality","district",
                 "is_valid","invalid_test","pesticide_name","conc_ppm",
                 "failed_tests_derived","validity_status","gso_category","sample_name_group"],
```

- [ ] **Step 3: Point `renderTopSubtypes` at the column and delete the JS grouper**

Delete `SAMPLE_NAME_FAMILY` (lines 1311-1323) and the `canonName` function (lines 1324-1331). In `renderTopSubtypes` change line 1341 from:

```javascript
      const n = canonName(r[COLS.sample_name]); if (!n) return;
```

to:

```javascript
      const n = r[COLS.sample_name_group] || r[COLS.sample_name]; if (!n) return;
```

- [ ] **Step 4: Rebuild and verify the JS duplicate is gone and the column is wired**

Run:
```bash
$PY scripts/build_dashboard.py
grep -c "canonName\|SAMPLE_NAME_FAMILY" reports/chemistry_dashboard.html
grep -c "sample_name_group" reports/chemistry_dashboard.html
```
Expected: first grep prints `0` (duplicate removed), second prints `>=1` (column present). Build prints no traceback.

- [ ] **Step 5: Verify top-subtypes payload has no dairy-as-fruit**

Create `chemistry/scripts/tests/verify_dashboard.py`:

```python
import re, json, sys
html = open("reports/chemistry_dashboard.html", encoding="utf-8").read()
m = re.search(r"const DATA = (\{.*?\});\n\s*const COLS", html, re.S)
assert m, "could not extract DATA payload"
DATA = json.loads(m.group(1))
cols = {c: i for i, c in enumerate(DATA["cols"])}
assert "sample_name_group" in cols, "sample_name_group missing from payload"
FRUIT = {"ليمون","برتقال","يوسفي","فراولة","تفاح","عنب","بصل","طماطم","خس","شطة","فلفل"}
DAIRY = "الحليب ومنتجات الألبان"   # C_DAIRY — payload slot "sample_category" is the canonical category
bad = []
for sec in DATA["sections"].values():
    for row in sec["rows"]:
        g = row[cols["sample_name_group"]]
        if row[cols["sample_category"]] == DAIRY and g in FRUIT:
            bad.append(row[cols["sample_name"]])
assert not bad, f"dairy rows grouped as fruit: {bad[:5]}"
print("DASHBOARD VERIFY OK")
```

Run: `$PY scripts/tests/verify_dashboard.py`
Expected: `DASHBOARD VERIFY OK`.

- [ ] **Step 6: Commit**

```bash
git add chemistry/scripts/build_dashboard.py chemistry/scripts/tests/verify_dashboard.py chemistry/reports/chemistry_dashboard.html
git commit -m "Dashboard: top-subtypes reads sample_name_group; delete JS canonName duplicate (#11)"
```

---

### Task 6: Fail-test labels — split water, merge arsenic, English canon (#5/#8/#12)

**Files:**
- Modify: `chemistry/scripts/build_dashboard.py` — `FAIL_LABEL_MAP` (1141-1169), `PLACEHOLDER_TOKENS` (1174), `renderFail` water branch (1211-1216), and the drilldown mirror (~1665)

**Interfaces:**
- Consumes: `normaliseFail(label)`, `FAIL_LABEL_MAP`, `rowSection(r)`, `COLS`.

- [ ] **Step 1: Extend `FAIL_LABEL_MAP`**

In `build_dashboard.py`, add these entries inside `FAIL_LABEL_MAP` (before its closing `};` at line 1169). Keys must be lowercase:

```javascript
    // #8 — arsenic variants merge to one label
    'arsenic':                  'Arsenic',
    'total arsenic':            'Arsenic',
    'الزرنيخ الكلي':            'Arsenic',
    // #5/#12 — water analytes → canonical English
    'sulphate':                 'Sulphate',
    'الكبريتات':                'Sulphate',
    'chloride':                 'Chloride',
    'nitrate':                  'Nitrate',
    'nitrate(no3)':             'Nitrate',
    'النترات':                  'Nitrate',
    'nitrite':                  'Nitrite',
    'floride':                  'Fluoride',
    'fluorid':                  'Fluoride',
    'fluoride':                 'Fluoride',
    'tds':                      'TDS',
    'total dissolved salt tds': 'TDS',
    't.hardness':               'Total hardness',
    'total hardness':           'Total hardness',
    'sodium':                   'Sodium',
    'ph':                       'pH',
    'turbidity':                'Turbidity',
```

- [ ] **Step 2: Add leaked subtype tokens to `PLACEHOLDER_TOKENS`**

Change line 1174 from:

```javascript
  const PLACEHOLDER_TOKENS = ['تراكيز المبيدات أقل','أقل من 0.01','below detection','below 0.01'];
```

to:

```javascript
  const PLACEHOLDER_TOKENS = ['تراكيز المبيدات أقل','أقل من 0.01','below detection','below 0.01',
                              'مياه فلتر','مياة فلتر','فلتر'];
```

- [ ] **Step 3: Split space-joined water failures in `renderFail`**

Water stores several failed analytes as one space-joined string. In `renderFail`, replace the non-pesticide branch (lines 1211-1216):

```javascript
      } else if (r[COLS.failed_tests_derived]) {
        // Per-test column may list multiple failures separated by '|'.
        String(r[COLS.failed_tests_derived]).split('|').forEach(t => add(t.trim()));
      } else if (r[COLS.is_valid] === 0 && r[COLS.invalid_test]) {
        add(r[COLS.invalid_test]);
      }
```

with (adds a water-only space-split; `WATER_TOKENS` keeps multi-word analyte names like "T.Hardness" intact):

```javascript
      } else if (sec === 'water_analysis') {
        // #12 — water lab records several failed analytes as ONE space-joined
        // string (e.g. "TDS T.Hardness Chloride Nitrate Sulphate Sodium").
        // Split into individual analytes so each is counted on its own.
        const raw = r[COLS.failed_tests_derived] || (r[COLS.is_valid] === 0 ? r[COLS.invalid_test] : '');
        splitWaterTests(raw).forEach(add);
      } else if (r[COLS.failed_tests_derived]) {
        // Per-test column may list multiple failures separated by '|'.
        String(r[COLS.failed_tests_derived]).split('|').forEach(t => add(t.trim()));
      } else if (r[COLS.is_valid] === 0 && r[COLS.invalid_test]) {
        add(r[COLS.invalid_test]);
      }
```

Then add the `splitWaterTests` helper just above `function renderFail()` (before line 1184):

```javascript
  // Multi-word water analyte names that must survive the space-split.
  const WATER_MULTIWORD = [
    [/total\s+dissolved\s+salt\s+tds/ig, 'TDS'],
    [/t\.?\s*hardness/ig, 'T.Hardness'],
    [/total\s+hardness/ig, 'T.Hardness'],
    [/nitrate\s*\(no3\)/ig, 'Nitrate'],
  ];
  function splitWaterTests(raw) {
    if (!raw) return [];
    let s = String(raw).replace(/\|/g, ' ');
    WATER_MULTIWORD.forEach(([re, tok]) => { s = s.replace(re, ' ' + tok + ' '); });
    return s.split(/\s+/).map(t => t.trim()).filter(Boolean);
  }
```

- [ ] **Step 4: Mirror the split in the drilldown**

The drilldown (~line 1665) re-derives the same issue tokens. Find the drilldown code that builds the per-row issue label from `failed_tests_derived` and, for `water_analysis` rows, route it through `splitWaterTests(...)` joined by `', '` so the drilldown text matches the chart. (Read lines 1641-1707 first; apply the same `sec === 'water_analysis'` branch used in Step 3, joining with `', '` for display.)

- [ ] **Step 5: Rebuild and verify labels**

Run:
```bash
$PY scripts/build_dashboard.py
grep -c "Total arsenic\|الزرنيخ الكلي" reports/chemistry_dashboard.html   # map keys still present is fine
$PY -c "
import re,json
html=open('reports/chemistry_dashboard.html',encoding='utf-8').read()
assert 'splitWaterTests' in html, 'splitWaterTests missing'
assert \"'total arsenic':\" in html, 'arsenic merge missing'
print('FAIL-LABEL WIRING OK')
"
```
Expected: `FAIL-LABEL WIRING OK`, build has no traceback.

- [ ] **Step 6: Visual check**

Open `reports/chemistry_dashboard.html`, select **Water analysis → non-potable** (Compliance filter or GSO chip): the "Top non-compliant tests" chart shows **individual** analytes (Chloride, Nitrate, Sulphate, TDS…), not one long joined bar. Select **Heavy metals**: a single **Arsenic** bar, no separate "Total arsenic".

- [ ] **Step 7: Commit**

```bash
git add chemistry/scripts/build_dashboard.py chemistry/reports/chemistry_dashboard.html
git commit -m "Dashboard: split water fail-tests, merge arsenic, English labels (#5/#8/#12)"
```

---

### Task 7: YoY card respects active filters (#9)

**Files:**
- Modify: `chemistry/scripts/build_dashboard.py` — `renderYoY` (1468-1554)

**Interfaces:**
- Consumes: `filteredRows()`, `getCombinedRows()`, `COLS`, `currentSection`, `isAllSections()`.

- [ ] **Step 1: Drive the per-section branch off `filteredRows()`**

In `renderYoY`, the per-section branch (lines 1526-1553) reads `sec.rows`. Replace its row source so it uses the current filter set. Change lines 1526-1535 from:

```javascript
    const sec = DATA.sections[currentSection];
    if (sec.years.length < 2) { card.style.display = 'none'; return; }
    card.style.display = '';
    const stats = {};
    sec.years.forEach(y => {
      const sub = sec.rows.filter(r => r[COLS.year] === y);
      const inv = sub.filter(r => r[COLS.is_valid] === 0).length;
      stats[y] = {total: sub.length, invalid: inv,
                  pct: sub.length ? inv * 100 / sub.length : 0};
    });
```

to:

```javascript
    const sec = DATA.sections[currentSection];
    if (sec.years.length < 2) { card.style.display = 'none'; return; }
    card.style.display = '';
    const fr = filteredRows();   // #9 — YoY must reflect compliance/sector/GSO/search
    const stats = {};
    sec.years.forEach(y => {
      const sub = fr.filter(r => r[COLS.year] === y);
      const inv = sub.filter(r => r[COLS.is_valid] === 0).length;
      stats[y] = {total: sub.length, invalid: inv,
                  pct: sub.length ? inv * 100 / sub.length : 0};
    });
```

- [ ] **Step 2: Drive the all-sections branch off `filteredRows()`**

In the `isAllSections()` branch, the per-section table rows come from `sec.rows` (lines 1483-1484). Replace with a filtered set keyed by section. Change lines 1481-1488 from:

```javascript
      const tr = Object.entries(DATA.sections).map(([key, sec]) => {
        if (!sec.years.includes(y1) || !sec.years.includes(y2)) return '';
        const r1 = sec.rows.filter(r => r[COLS.year] === y1);
        const r2 = sec.rows.filter(r => r[COLS.year] === y2);
```

to:

```javascript
      const frAll = filteredRows();   // #9 — respect active filters
      const bySec = {};
      frAll.forEach(r => { const s = rowSection(r); (bySec[s] = bySec[s] || []).push(r); });
      const tr = Object.entries(DATA.sections).map(([key, sec]) => {
        if (!sec.years.includes(y1) || !sec.years.includes(y2)) return '';
        const secRows = bySec[key] || [];
        const r1 = secRows.filter(r => r[COLS.year] === y1);
        const r2 = secRows.filter(r => r[COLS.year] === y2);
```

The aggregate rows below already use `getCombinedRows()`; leave those — but note `getCombinedRows()` is itself filtered (confirm during Step 3). If it is not, replace `getCombinedRows()` on lines 1505-1506 with `filteredRows()`.

- [ ] **Step 3: Rebuild and confirm the wiring**

Run:
```bash
$PY scripts/build_dashboard.py
$PY -c "html=open('reports/chemistry_dashboard.html',encoding='utf-8').read(); assert html.count('#9 — ')>=2 or 'YoY must reflect' in html; print('YOY WIRING OK')"
```
Expected: `YOY WIRING OK`, no build traceback.

- [ ] **Step 4: Visual check**

Open the dashboard, pick a section with both years, note the YoY numbers, then toggle a **Sector** chip (e.g. Central). The YoY sample counts and % must change. Reset filters → numbers return to the full totals.

- [ ] **Step 5: Commit**

```bash
git add chemistry/scripts/build_dashboard.py chemistry/reports/chemistry_dashboard.html
git commit -m "Dashboard: YoY comparison respects active filters (#9)"
```

---

### Task 8: "Unspecified" label for invalid rows with no derivable test (#10)

**Files:**
- Investigate: raw food-chem source for عصير برتقال
- Modify: `chemistry/scripts/build_dashboard.py` — `renderFail` fallback (after line 1216)

**Interfaces:**
- Consumes: `renderFail`, `COLS`, `add(label)`.

- [ ] **Step 1: Investigate whether a test is derivable**

Run:
```bash
$PY -c "
import pandas as pd, glob
A=pd.concat([pd.read_parquet(f) for f in glob.glob('cleaned/chem_food_chemistry_*.parquet')])
oj=A[(A['sample_name'].astype(str).str.contains('عصير برتقال',na=False)) & (A['is_valid']==False)]
cols=[c for c in A.columns if c.endswith('_value') or c.endswith('_limit_value') or c in ('invalid_test','failed_tests_derived','validity_raw')]
print(oj[['sample_id']+cols].dropna(axis=1,how='all').to_string())
"
```
Expected: shows the row. If a `*_value` exceeds its `*_limit_value`, the cleaner missed a derivation — note it for a follow-up cleaner fix (out of scope here). If all limits are null / no value exceeds, the label genuinely can't be derived → proceed to Step 2.

- [ ] **Step 2: Add the "Unspecified" fallback**

In `renderFail`, the non-pesticide branch currently drops invalid rows that have neither `failed_tests_derived` nor `invalid_test`. Add a final `else` so such rows are still visible. After the `else if (r[COLS.is_valid] === 0 && r[COLS.invalid_test])` block (added/edited in Task 6), append:

```javascript
      } else if (r[COLS.is_valid] === 0) {
        // #10 — invalid but the lab recorded no failing test.
        add('Unspecified');
      }
```

- [ ] **Step 3: Rebuild and verify**

Run:
```bash
$PY scripts/build_dashboard.py
$PY -c "html=open('reports/chemistry_dashboard.html',encoding='utf-8').read(); assert \"add('Unspecified')\" in html; print('UNSPECIFIED OK')"
```
Expected: `UNSPECIFIED OK`.

- [ ] **Step 4: Visual check**

Open the dashboard → Food chemistry (or All sections), Compliance = Non-compliant. Confirm an **"Unspecified"** bar now appears (small) and the عصير برتقال sample is in the drilldown.

- [ ] **Step 5: Commit**

```bash
git add chemistry/scripts/build_dashboard.py chemistry/reports/chemistry_dashboard.html
git commit -m "Dashboard: label invalid-but-no-test rows as Unspecified (#10)"
```

---

### Task 9: Final full rebuild + end-to-end verification

**Files:**
- Regenerate: all parquets, mirror, dashboard
- Run: both verify scripts

- [ ] **Step 1: Clean rebuild everything**

Run:
```bash
$PY scripts/clean_chemistry.py --section all
cp cleaned/*.parquet ../clean/chemistry/
$PY scripts/build_dashboard.py
```
Expected: 12 parquet writes + dashboard build, no traceback.

- [ ] **Step 2: Run all verification scripts**

Run:
```bash
$PY scripts/tests/test_categories.py
$PY scripts/tests/verify_data.py
$PY scripts/tests/verify_dashboard.py
```
Expected: `ALL PASS`, `DATA VERIFY OK`, `DASHBOARD VERIFY OK`.

- [ ] **Step 3: Manual acceptance pass**

Open `reports/chemistry_dashboard.html` and confirm each fix against the spec's Verification list: #1/#2 nameless water named, #8 single Arsenic bar, #9 YoY reacts to filters, #11 no fruit label under dairy, #12 split water analytes, #13 no Others bucket, #14 كشنة under spices. Spot-check meats/fish/filter-water collapse in the top-subtypes table.

- [ ] **Step 4: Final commit**

```bash
git add chemistry/cleaned/*.parquet clean/chemistry/*.parquet chemistry/reports/chemistry_dashboard.html
git commit -m "Chem: final rebuild — 14-point dashboard corrections (jam #15 deferred)"
```

---

## Notes / follow-ups
- **#15 Jam (المربى):** deferred — implement when Amjad's spreadsheet arrives (new corrections into `category_corrections.csv` or a `NAME_OVERRIDE`/family rule).
- If Step 1 of Task 8 shows a derivable exceedance, open a separate cleaner-side task to capture it in `failed_tests_derived` rather than relying on the "Unspecified" fallback.
